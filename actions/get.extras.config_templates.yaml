# This action was auto generated from the NetBox API Swagger Spec
# NetBox API version: 4.6
description: "Get a list of config template objects."
enabled: true
entry_point: run.py
name: get.extras.config_templates
parameters:
  endpoint_uri:
    default: "/extras/config-templates/"
    immutable: true
    type: string
  http_verb:
    default: get
    immutable: true
    type: string
  get_detail_route_eligible:
    default: true
    immutable: true
    type: boolean
  fail_non_2xx:
    type: boolean
    description: Set action as failed when http return reponse status isn't 2xx.
  as_attachment:
    required: false
    type: boolean
    description: "As_attachment"
  auto_sync_enabled:
    required: false
    type: boolean
    description: "Auto_sync_enabled"
  created:
    required: false
    type: string
    description: "Created"
  created__empty:
    required: false
    type: string
    description: "Created is empty/null (boolean)"
  created__gt:
    required: false
    type: string
    description: "Created greater than"
  created__gte:
    required: false
    type: string
    description: "Created greater than or equal to"
  created__lt:
    required: false
    type: string
    description: "Created less than"
  created__lte:
    required: false
    type: string
    description: "Created less than or equal to"
  created__n:
    required: false
    type: string
    description: "Created not equal to"
  created_by_request:
    required: false
    type: string
    description: "Created_by_request"
  data_file_id:
    required: false
    type: integer
    description: "Data file (ID)"
  data_file_id__n:
    required: false
    type: integer
    description: "Data_file_id not equal to"
  data_source_id:
    required: false
    type: integer
    description: "Data source (ID)"
  data_source_id__n:
    required: false
    type: integer
    description: "Data_source_id not equal to"
  data_synced:
    required: false
    type: string
    description: "Data_synced"
  data_synced__empty:
    required: false
    type: boolean
    description: "Data_synced is empty/null (boolean)"
  data_synced__gt:
    required: false
    type: string
    description: "Data_synced greater than"
  data_synced__gte:
    required: false
    type: string
    description: "Data_synced greater than or equal to"
  data_synced__lt:
    required: false
    type: string
    description: "Data_synced less than"
  data_synced__lte:
    required: false
    type: string
    description: "Data_synced less than or equal to"
  data_synced__n:
    required: false
    type: string
    description: "Data_synced not equal to"
  debug:
    required: false
    type: boolean
    description: "Debug"
  description:
    required: false
    type: string
    description: "Description"
  description__empty:
    required: false
    type: boolean
    description: "Description is empty/null (boolean)"
  description__ic:
    required: false
    type: string
    description: "Description contains (case-insensitive)"
  description__ie:
    required: false
    type: string
    description: "Description exact match (case-insensitive)"
  description__iew:
    required: false
    type: string
    description: "Description ends with (case-insensitive)"
  description__iregex:
    required: false
    type: string
    description: "Description"
  description__isw:
    required: false
    type: string
    description: "Description starts with (case-sensitive)"
  description__n:
    required: false
    type: string
    description: "Description not equal to"
  description__nic:
    required: false
    type: string
    description: "Description does not contain (case-insensitive)"
  description__nie:
    required: false
    type: string
    description: "Description inverse exact match (case-insensitive)"
  description__niew:
    required: false
    type: string
    description: "Description does not end with (case-insensitive)"
  description__nisw:
    required: false
    type: string
    description: "Description does not start with (case-sensitive)"
  description__regex:
    required: false
    type: string
    description: "Description"
  file_extension:
    required: false
    type: string
    description: "File_extension"
  file_extension__empty:
    required: false
    type: boolean
    description: "File_extension is empty/null (boolean)"
  file_extension__ic:
    required: false
    type: string
    description: "File_extension contains (case-insensitive)"
  file_extension__ie:
    required: false
    type: string
    description: "File_extension exact match (case-insensitive)"
  file_extension__iew:
    required: false
    type: string
    description: "File_extension ends with (case-insensitive)"
  file_extension__iregex:
    required: false
    type: string
    description: "File_extension"
  file_extension__isw:
    required: false
    type: string
    description: "File_extension starts with (case-sensitive)"
  file_extension__n:
    required: false
    type: string
    description: "File_extension not equal to"
  file_extension__nic:
    required: false
    type: string
    description: "File_extension does not contain (case-insensitive)"
  file_extension__nie:
    required: false
    type: string
    description: "File_extension inverse exact match (case-insensitive)"
  file_extension__niew:
    required: false
    type: string
    description: "File_extension does not end with (case-insensitive)"
  file_extension__nisw:
    required: false
    type: string
    description: "File_extension does not start with (case-sensitive)"
  file_extension__regex:
    required: false
    type: string
    description: "File_extension"
  file_name:
    required: false
    type: string
    description: "File_name"
  file_name__empty:
    required: false
    type: boolean
    description: "File_name is empty/null (boolean)"
  file_name__ic:
    required: false
    type: string
    description: "File_name contains (case-insensitive)"
  file_name__ie:
    required: false
    type: string
    description: "File_name exact match (case-insensitive)"
  file_name__iew:
    required: false
    type: string
    description: "File_name ends with (case-insensitive)"
  file_name__iregex:
    required: false
    type: string
    description: "File_name"
  file_name__isw:
    required: false
    type: string
    description: "File_name starts with (case-sensitive)"
  file_name__n:
    required: false
    type: string
    description: "File_name not equal to"
  file_name__nic:
    required: false
    type: string
    description: "File_name does not contain (case-insensitive)"
  file_name__nie:
    required: false
    type: string
    description: "File_name inverse exact match (case-insensitive)"
  file_name__niew:
    required: false
    type: string
    description: "File_name does not end with (case-insensitive)"
  file_name__nisw:
    required: false
    type: string
    description: "File_name does not start with (case-sensitive)"
  file_name__regex:
    required: false
    type: string
    description: "File_name"
  id:
    required: false
    type: integer
    description: "Id"
  id__empty:
    required: false
    type: boolean
    description: "Id is empty/null (boolean)"
  id__gt:
    required: false
    type: integer
    description: "Id greater than"
  id__gte:
    required: false
    type: integer
    description: "Id greater than or equal to"
  id__lt:
    required: false
    type: integer
    description: "Id less than"
  id__lte:
    required: false
    type: integer
    description: "Id less than or equal to"
  id__n:
    required: false
    type: integer
    description: "Id not equal to"
  last_updated:
    required: false
    type: string
    description: "Last_updated"
  last_updated__empty:
    required: false
    type: string
    description: "Last_updated is empty/null (boolean)"
  last_updated__gt:
    required: false
    type: string
    description: "Last_updated greater than"
  last_updated__gte:
    required: false
    type: string
    description: "Last_updated greater than or equal to"
  last_updated__lt:
    required: false
    type: string
    description: "Last_updated less than"
  last_updated__lte:
    required: false
    type: string
    description: "Last_updated less than or equal to"
  last_updated__n:
    required: false
    type: string
    description: "Last_updated not equal to"
  limit:
    required: false
    type: integer
    description: "Number of results to return per page."
  mime_type:
    required: false
    type: string
    description: "Mime_type"
  mime_type__empty:
    required: false
    type: boolean
    description: "Mime_type is empty/null (boolean)"
  mime_type__ic:
    required: false
    type: string
    description: "Mime_type contains (case-insensitive)"
  mime_type__ie:
    required: false
    type: string
    description: "Mime_type exact match (case-insensitive)"
  mime_type__iew:
    required: false
    type: string
    description: "Mime_type ends with (case-insensitive)"
  mime_type__iregex:
    required: false
    type: string
    description: "Mime_type"
  mime_type__isw:
    required: false
    type: string
    description: "Mime_type starts with (case-sensitive)"
  mime_type__n:
    required: false
    type: string
    description: "Mime_type not equal to"
  mime_type__nic:
    required: false
    type: string
    description: "Mime_type does not contain (case-insensitive)"
  mime_type__nie:
    required: false
    type: string
    description: "Mime_type inverse exact match (case-insensitive)"
  mime_type__niew:
    required: false
    type: string
    description: "Mime_type does not end with (case-insensitive)"
  mime_type__nisw:
    required: false
    type: string
    description: "Mime_type does not start with (case-sensitive)"
  mime_type__regex:
    required: false
    type: string
    description: "Mime_type"
  modified_by_request:
    required: false
    type: string
    description: "Modified_by_request"
  name:
    required: false
    type: string
    description: "Name"
  name__empty:
    required: false
    type: boolean
    description: "Name is empty/null (boolean)"
  name__ic:
    required: false
    type: string
    description: "Name contains (case-insensitive)"
  name__ie:
    required: false
    type: string
    description: "Name exact match (case-insensitive)"
  name__iew:
    required: false
    type: string
    description: "Name ends with (case-insensitive)"
  name__iregex:
    required: false
    type: string
    description: "Name"
  name__isw:
    required: false
    type: string
    description: "Name starts with (case-sensitive)"
  name__n:
    required: false
    type: string
    description: "Name not equal to"
  name__nic:
    required: false
    type: string
    description: "Name does not contain (case-insensitive)"
  name__nie:
    required: false
    type: string
    description: "Name inverse exact match (case-insensitive)"
  name__niew:
    required: false
    type: string
    description: "Name does not end with (case-insensitive)"
  name__nisw:
    required: false
    type: string
    description: "Name does not start with (case-sensitive)"
  name__regex:
    required: false
    type: string
    description: "Name"
  offset:
    required: false
    type: integer
    description: "The initial index from which to return the results."
  ordering:
    required: false
    type: string
    description: "Which field to use when ordering the results."
  owner:
    required: false
    type: string
    description: "Owner (name)"
  owner__n:
    required: false
    type: string
    description: "Owner not equal to"
  owner_group:
    required: false
    type: string
    description: "Owner Group (name)"
  owner_group__n:
    required: false
    type: string
    description: "Owner_group not equal to"
  owner_group_id:
    required: false
    type: integer
    description: "Owner Group (ID)"
  owner_group_id__n:
    required: false
    type: integer
    description: "Owner_group_id not equal to"
  owner_id:
    required: false
    type: integer
    description: "Owner (ID)"
  owner_id__n:
    required: false
    type: integer
    description: "Owner_id not equal to"
  q:
    required: false
    type: string
    description: "Search"
  start:
    required: false
    type: integer
    description: "Cursor-based pagination: return results with pk &gt;= start, ordered by pk. Mutually exclusive with offset."
  tag:
    required: false
    type: string
    description: "Tag"
  tag__n:
    required: false
    type: string
    description: "Tag not equal to"
  tag_id:
    required: false
    type: integer
    description: "Tag_id"
  tag_id__n:
    required: false
    type: integer
    description: "Tag_id not equal to"
  updated_by_request:
    required: false
    type: string
    description: "Updated_by_request"
  save_in_key_store:
    type: boolean
    default: false
    description: Save the result of the action as a json object in the st2 key store. Used when the expected result from Netbox is very large and the result will be piped to another action. You must also specify a save_in_key_store_keyname and an optional save_in_key_store_ttl.
  save_in_key_store_key_name:
    type: string
    description: Name of the key to store the json result value in the st2 key store. Must be used with save_in_key_store and optionally save_in_key_store_ttl.
  save_in_key_store_ttl:
    type: integer
    default: 60
    description: TTL (seconds) of the saved json result in the st2 key store. Defaults to 60 seconds. Must be used with save_in_key_store and save_in_key_store_key_name.
runner_type: python-script
