# This action was auto generated from the NetBox API Swagger Spec
# NetBox API version: 3.7
description: "Post a list of service template objects."
enabled: true
entry_point: run.py
name: post.ipam.service_templates
parameters:
  endpoint_uri:
    default: "/ipam/service-templates/"
    immutable: true
    type: string
  http_verb:
    default: post
    immutable: true
    type: string
  get_detail_route_eligible:
    default: true
    immutable: true
    type: boolean
  name:
    required: false
    type: string
    description: "Name"
  ports:
    required: false
    type: array
    description: "Port numbers"
  protocol:
    required: false
    type: string
    description: "* `tcp` - TCP
* `udp` - UDP
* `sctp` - SCTP"
  description:
    required: false
    type: string
    description: "Description"
  comments:
    required: false
    type: string
    description: "Comments"
  tags:
    required: false
    type: array
    description: "Array of tag strings"
  custom_fields:
    required: false
    type: object
    description: "Custom fields"
runner_type: python-script
