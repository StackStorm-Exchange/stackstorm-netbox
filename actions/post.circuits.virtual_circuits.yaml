# This action was auto generated from the NetBox API Swagger Spec
# NetBox API version: 4.6
description: "Post a list of virtual circuit objects."
enabled: true
entry_point: run.py
name: post.circuits.virtual_circuits
parameters:
  endpoint_uri:
    default: "/circuits/virtual-circuits/"
    immutable: true
    type: string
  http_verb:
    default: post
    immutable: true
    type: string
  get_detail_route_eligible:
    default: true
    immutable: true
    type: boolean
  fail_non_2xx:
    type: boolean
    description: Set action as failed when http return reponse status isn't 2xx.
  cid:
    required: false
    type: string
    description: "Circuit ID"
  provider_network:
    required: false
    type: integer
    description: "Provider network"
  provider_account:
    required: false
    type: integer
    description: "Provider account"
  type:
    required: false
    type: integer
    description: "Type"
  status:
    required: false
    type: string
    description: "* `planned` - Planned
* `provisioning` - Provisioning
* `active` - Active
* `offline` - Offline
* `deprovisioning` - Deprovisioning
* `decommissioned` - Decommissioned"
  tenant:
    required: false
    type: integer
    description: "Tenant"
  description:
    required: false
    type: string
    description: "Description"
  owner:
    required: false
    type: integer
    description: "Owner"
  comments:
    required: false
    type: string
    description: "Comments"
  tags:
    required: false
    type: array
    description: "Array of tag strings"
  custom_fields:
    required: false
    type: object
    description: "Custom fields"
runner_type: python-script
